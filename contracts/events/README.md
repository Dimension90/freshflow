# Event schemas

Machine-readable JSON Schema 2020-12 contracts cover the common envelope and every event payload implemented so far. Producers persist the complete envelope in their transactional outbox before Kafka publication.
